#The following formula is an adaptation of the one used in https://www.ncbi.nlm.nih.gov/pmc/articles/PMC169007/

As = []
p = 3.14
for atom in all_atoms:
    Ai = 1
    for atom in all_atoms:
        if dist > (R_i + R_j + (2*R_solv))
            Ai *= 1 - 0
        else dist <= (R_i + R_j + (2*R_solv))

    As.append((4*p*((R_i + R_solv)**2)) * Ai)

SASA = sum(As)
# map function
